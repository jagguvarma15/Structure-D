---
import BaseLayout from '../layouts/BaseLayout.astro';
import Navbar from '../components/Navbar.astro';
import '../styles/global.css';

const base = import.meta.env.BASE_URL;
---

<BaseLayout title="404 — Page Not Found">
  <Navbar />

  <main class="not-found">
    <div class="container not-found-inner">
      <div class="not-found-terminal reveal">
        <div class="terminal-titlebar">
          <div class="terminal-dots">
            <div class="terminal-dot red"></div>
            <div class="terminal-dot yellow"></div>
            <div class="terminal-dot green"></div>
          </div>
          <span class="terminal-title">bash</span>
        </div>
        <div class="terminal-body">
          <div class="terminal-line">
            <span class="terminal-prompt">$</span>
            <span class="terminal-cmd">structure-d extract this_page.html --schema key_value</span>
          </div>
          <span class="terminal-output"><svg width="14" height="14" viewBox="0 0 16 16" fill="currentColor" aria-hidden="true"><path d="M8.982 1.566a1.13 1.13 0 0 0-1.96 0L.165 13.233c-.457.778.091 1.767.98 1.767h13.713c.889 0 1.438-.99.98-1.767L8.982 1.566zM8 5c.535 0 .954.462.9.995l-.35 3.507a.552.552 0 0 1-1.1 0L7.1 5.995A.905.905 0 0 1 8 5zm.002 6a1 1 0 1 1 0 2 1 1 0 0 1 0-2z"/></svg> ParserError: file not found — this_page.html</span>
          <span class="terminal-output">  File path: /docs/this_page</span>
          <span class="terminal-output">  HTTP status: <span style="color: var(--error)">404</span></span>
        </div>
      </div>

      <div class="not-found-content reveal" style="transition-delay: 100ms">
        <p class="not-found-code">404</p>
        <h1 class="not-found-title">Page not found</h1>
        <p class="not-found-desc">
          The page you're looking for doesn't exist or has been moved.
          Check the URL or navigate back to safety.
        </p>
        <div class="not-found-actions">
          <a href={base} class="btn btn-primary">← Back to Home</a>
          <a href={`${base}docs/getting-started`} class="btn btn-secondary">Docs</a>
        </div>
      </div>
    </div>
  </main>
</BaseLayout>

<style>
  .not-found {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding-top: var(--nav-height);
  }

  .not-found-inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-7);
    align-items: center;
    max-width: 900px;
  }

  .not-found-terminal {
    max-width: 420px;
  }

  .not-found-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .not-found-code {
    font-family: var(--font-mono);
    font-size: 5rem;
    font-weight: 600;
    line-height: 1;
    background: linear-gradient(135deg, var(--accent) 0%, #a78bfa 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .not-found-title {
    font-size: var(--text-2xl);
    font-weight: 600;
    color: var(--text-primary);
    letter-spacing: -0.02em;
  }

  .not-found-desc {
    font-size: var(--text-base);
    color: var(--text-secondary);
    line-height: 1.7;
    max-width: 380px;
  }

  .not-found-actions {
    display: flex;
    gap: var(--space-3);
    margin-top: var(--space-2);
    flex-wrap: wrap;
  }

  @media (max-width: 768px) {
    .not-found-inner {
      grid-template-columns: 1fr;
      text-align: center;
      gap: var(--space-5);
    }
    .not-found-terminal { max-width: 100%; }
    .not-found-desc { max-width: 100%; }
    .not-found-actions { justify-content: center; }
  }
</style>
